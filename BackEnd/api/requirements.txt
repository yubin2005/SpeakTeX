# AWS SDK for Python
boto3>=1.34.0
botocore>=1.34.0

# Environment variable management
python-dotenv>=1.0.0

# Type hints and validation (for service classes)
pydantic>=2.8.0

# Utilities
python-dateutil>=2.8.2

# Note: Flask dependencies removed - using serverless Lambda architecture
# This requirements.txt is kept for the utility services (aws_config, s3_service)
